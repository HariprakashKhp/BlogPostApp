<%- include("partials/header"); -%>

<% for(let i = 0; i< posts.length; i++) { %>
    <a class="link" href="/posts/<%= posts[i]._id %>"><h1><%= posts[i].title %></h1></a>
    <p>
            <%= posts[i].body.substring(0, 100) + ". .." %> 
            <a href="/posts/<%= posts[i]._id %>">Read More</a>
    </p>
<% } %>

<%- include("partials/footer"); -%>
